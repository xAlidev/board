import React from 'react';

const Header = () => {
    return (
        <header className="bg-gray-800 text-white h-16 flex items-center px-4">
            <h2 className="text-lg font-semibold">Dashboard Header</h2>
        </header>
    );
};

export default Header;
